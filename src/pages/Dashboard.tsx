const Dashboard = () => {
  return (
    <section
      id="dashboard"
      className="min-h-screen flex flex-col items-center justify-center py-20"
    >
      <div className="text-white text-2xl mb-6">Welcome to Dashboard</div>
    </section>
  );
};

export default Dashboard;
